{
  "buildCommand": "cd Front-end-Front-Office- && npm ci --legacy-peer-deps && npm run build",
  "outputDirectory": "Front-end-Front-Office-/build",
  "installCommand": "echo 'Skipping root install; frontend dependencies are installed in buildCommand'",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}