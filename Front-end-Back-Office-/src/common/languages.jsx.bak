// Some flag images may be missing in the assets for certain locales.
// Use an available default flag (US) as a safe fallback so the build doesn't fail.
import defaultFlag from "../assets/images/flags/us.jpg"

const languages = {
  in: {
    label: "India",
    flag: defaultFlag,
  },
}

export default languages